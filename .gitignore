# Python / runtime
__pycache__/
*.pyc
.venv/
venv/

# Build artifacts (large; built in CI or via tools/*.sh)
dist/
tools/stage/

# The fp32 source model is large and fetched from HF in CI (tools/fetch_model.sh).
ethio-asr/

# Test / scratch output
output/

# ad-hoc panel backups (large)
backup_installed_*/

# Node deps
node_modules/

# Wrangler local scratch
tools/telegram-worker/.wrangler/

# Telegram bot secret config (token + AMH_SECRET + webhook secret) — NEVER commit
tools/telegram/bot.env

# Telegram bot runtime log
tools/telegram/bot_live.log

# Local license/customer ledgers (may contain bearer keys)
tools/**/customers.csv
tools/**/customers.csv.tmp
tools/telegram/fsm.json
tools/telegram/contacts.json
tools/telegram/funnel.csv
tools/telegram/pending.json

# OS cruft
.DS_Store
